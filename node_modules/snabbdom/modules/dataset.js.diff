diff --git a/modules/dataset.js b/modules/dataset.js
index 37e573b..bce15e3 100644
--- a/modules/dataset.js
+++ b/modules/dataset.js
@@ -1,13 +1,10 @@
+"use strict";
 function updateDataset(oldVnode, vnode) {
-  var elm = vnode.elm,
-    oldDataset = oldVnode.data.dataset,
-    dataset = vnode.data.dataset,
-    key
-
-  if (!oldDataset && !dataset) return;
+  var elm = vnode.elm, oldDataset = oldVnode.data.dataset, dataset = vnode.data.dataset, key;
+  if (!oldDataset && !dataset)
+    return;
   oldDataset = oldDataset || {};
   dataset = dataset || {};
-
   for (key in oldDataset) {
     if (!dataset[key]) {
       delete elm.dataset[key];
@@ -19,5 +16,9 @@ function updateDataset(oldVnode, vnode) {
     }
   }
 }
-
-module.exports = {create: updateDataset, update: updateDataset}
+var datasetModule = {
+  create: updateDataset,
+  update: updateDataset,
+};
+module.exports = datasetModule;
+//# sourceMappingURL=dataset.js.map
\ No newline at end of file
